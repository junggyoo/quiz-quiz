import { screen } from '@testing-library/react';
import '@testing-library/jest-dom';

describe('CheckCorrectAnswer Component', () => {
  it('정답을 체크하면 정답이 맞는지 확인할 수 있다.', () => {
    expect(true).toBe(true);
  });
});
